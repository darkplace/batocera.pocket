dl/
output/
buildroot-ccache/
.ba-docker-image-available
**/__pycache__/
*.py[cod]
*~
keys.txt
# Private EmulationStation credentials; never publish this package-local file.
package/batocera/emulationstation/batocera-emulationstation/keys.txt
package/batocera/emulators/fex-emu/.host-bin/

# Local build locks / PIDs
*.pid
.build-pid
.docker-pid
.rebuild-lock-pid
.rebuild-*-pid

# Build / docker logs (never push)
*.log
*.log.bak*
build-exit*
build-exit-code*.txt
docker-build.log
docker-rebuild.log
docker-sm*-build.log*
docker-sm*-OK
docker-sm*-FAILED
.docker-sm*
.tmp-*

# Private local-only: upstream sync / package lifecycle tooling + state
# (reports live under output/ which is already ignored)
scripts/dev/upstream/
local.mk
.local/

# Private RC / local OTA bundles (boot.tar.xz ~multi-GB; never publish)
releases/private/

# Maintainer device scripts (LAN IPs / default passwords — never publish)
scripts/dev/apply-sm8750-ota.sh
scripts/dev/smoke-emu-odin3.sh
scripts/dev/package-private-rc-ota.sh

# Local Cursor rules / agent noise
.cursor/

# Ephemeral rebuild / smoke artifacts at repo root
rebuild-*.out
rpcs3-spu-*.png
*.DONE
check_hydra_sources.sh
hydra_sources_complete.json
HYDRA_SOURCES_*.md
