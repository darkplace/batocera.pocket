# Steam / Proton / FEX need a high map count (SteamOS parity; ROCKNIX #3141).
vm.max_map_count = 2147483642
# RPCS3 (and other emulators) reserve large virtual address ranges; heuristic
# overcommit rejects ~32GiB mmaps on handhelds with no swap.
vm.overcommit_memory = 1
